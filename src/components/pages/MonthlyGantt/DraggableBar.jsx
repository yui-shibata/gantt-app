import React from 'react';
import { useDrag } from 'react-dnd';

const DraggableBar = ({ name, info }) => {
  // const [{ isDragging }, drag] = useDrag({
  //   item: { type: 'highcharts-bar'},
  //   collect: (monitor) => ({
  //     isDragging: monitor.isDragging()
  //   })
  // });

  return (
    <p>sss</p>
  );
};

export default DraggableBar;